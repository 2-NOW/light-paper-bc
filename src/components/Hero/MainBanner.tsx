import { Box } from '@mui/material';

function MainBanner() {
  return (
    <Box sx={{ mt: '5rem', border: '1px solid', height: '50vh' }}>
      MainBanner
    </Box>
  );
}

export default MainBanner;
