import { Typography } from '@mui/material';

function index() {
  return (
    <Typography sx={{ mt: '10rem', mb: '10rem' }} variant="h2">
      Contact
    </Typography>
  );
}

export default index;
